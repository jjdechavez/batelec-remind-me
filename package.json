{
  "name": "batelec-remind-me",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "nodemon",
    "build": "rm -rf build && esbuild `find src \\( -name '*.ts' \\)` --platform=node --outdir=build --bundle",
    "start": "build && node build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@hapi/hapi": "^21.3.0",
    "joi": "^17.7.1"
  },
  "devDependencies": {
    "@types/hapi": "^18.0.8",
    "@types/node": "^18.14.0",
    "esbuild": "^0.17.8",
    "nodemon": "^2.0.20",
    "typescript": "^4.9.5"
  }
}
